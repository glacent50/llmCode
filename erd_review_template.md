# 예술누림 DB 스키마 리뷰 초안

이 문서는 제공된 ERD를 바탕으로 구조적 관찰 포인트와 점검 체크리스트를 정리하기 위한 템플릿입니다. 현재 스크린샷 해상도가 낮아 테이블/컬럼 식별이 어려우므로, 고해상도 ERD(PDF/PNG) 또는 DDL을 받는 즉시 구체 항목을 채워 넣겠습니다.

---

## 1) 시스템 개요 요약
- 도메인: 예술/문화 프로그램(추정)
- 핵심 개체(관찰 기반, 텍스트 미확인):
  - 중앙 허브형 테이블 1개: 컬럼 수가 매우 많고 여러 테이블과 1:N 관계로 연결됨
  - 주변 참조/코드형 테이블 다수: 마스터/코드값 저장 추정
  - 빨간색 하이라이트 테이블 1개: 중요/이상 징후 표시(삭제 플래그, 로그, 상태전이 등) 가능성
- 관계 패턴: 허브-스포크(Star-like) 구조 추정, 다수의 외래키가 허브를 가리킴

## 2) 엔터티 목록(초안)
아래는 자리표시자입니다. 고해상도 자료 수신 시 정확 명칭/컬럼/키/설명을 채웁니다.

- [엔터티-01 중앙 허브]
  - 키: (ID)
  - 주요 컬럼: (...)
  - 설명: 업무의 중심 트랜잭션/신청/작품/행사 등 중 하나일 가능성

- [엔터티-02 코드/마스터 A]
  - 키: (Code)
  - 설명: 분류/상태/유형 코드 관리

- [엔터티-03 코드/마스터 B]
  - 키: (Code)

- [엔터티-04 로그/상태/이력(빨간색)]
  - 키: (...)
  - 설명: 삭제/승인/검수/정산 등 상태 전이 이력일 수 있음

(필요 시 계속 추가)

## 3) 관계(관계도)
- 엔터티-02..N → 엔터티-01: 다대일(코드 참조)
- 엔터티-01 → 엔터티-04: 일대다(이력/상태/첨부 로그 등)
- 선택/점선 관계가 보이는 경우: 선택적(Nullable FK) 또는 약한 의존 가능성

고해상도 자료 확보 후 다음 정보를 확정합니다.
- 카디널리티(1:1, 1:N, N:M)
- 필수/선택(Null 허용 여부)
- 삭제/갱신 규칙(On Delete/Update Cascade/Restrict 등)

## 4) 키 설계 점검
- 기본키: 모든 테이블 고유성 보장 여부(의미없는 대리키 vs 의미있는 자연키)
- 유니크 제약: 후보키/식별자 중복 방지 규칙 존재 여부
- 외래키: 모든 참조 컬럼에 FK 제약이 실제로 존재하는지(무제약 조인 방지)

## 5) 정규화/모델링 품질
- 1NF: 허브 테이블 내 반복/집합 컬럼(예: col1, col2, col3…), CSV 문자열, JSON 컬럼 존재 여부
- 2NF/3NF: 부분/추이적 종속 제거(코드값의 이름/설명은 마스터 테이블로 분리)
- 지나친 정규화 여부: 조회경로가 과도하게 깊어지는지(성능/개발 생산성 균형)
- 넓은 테이블 스멜: 30~50+ 컬럼, 다목적(“신청/작품/행사/정산 등”이 한곳) 혼합 여부 → 수평/수직 분할 고려

## 6) 성능/인덱스 전략
- FK 컬럼 인덱스: 조인/삭제 성능 위해 FK 컬럼에 인덱스 부여 여부 점검
- 조회 패턴 기준 복합 인덱스 제안: (상태, 날짜), (유형, 지역), (사용자, 날짜) 등 실제 쿼리 플랜 기반 결정
- 긴 텍스트/파일 경로 컬럼: 별도 테이블/스토리지 분리 고려
- 대용량 이력/로그 테이블: 파티셔닝(범위/해시), 보관/아카이브 정책 명시

## 7) 무결성/비즈니스 규칙
- 상태 전이: 허용 전이만 가능하도록 체크 제약/트리거/애플리케이션 레벨 검증 정의
- 금액/수량: 음수 방지, 합계=상세 합산, 통화/부가세 규칙 등
- 중복 방지: (사용자, 기간, 대상) 유니크키로 중복 신청/등록 차단 등

## 8) 보안/권한/감사
- PII: 주민번호/연락처/주소 등 암호화/마스킹/열 권한
- 소유권 필드(owner_id)와 행 수준 접근제어(Row-Level Security) 고려
- 변경 이력(Audit): 생성/수정자, 타임스탬프, 논리삭제 여부

## 9) 네이밍/일관성 가이드
- 테이블: snake_case 복수형 또는 도메인 용어 표준화
- 컬럼 접두어: *_id, *_code, *_at(시간), is_* (Boolean)
- 코드 테이블: code, name, sort_order, use_yn 등 공통 스키마 권장

## 10) 마이그레이션/운영
- 외래키/인덱스 생성 순서와 다운타임 최소화 전략
- 참조 무결성 위반 데이터 정리 절차
- 샤딩/멀티테넌시가 필요할 경우 테넌트 키 설계

## 11) 우선 확인이 필요한 자료
아래 중 하나를 공유해 주세요. 받는 즉시 본 문서를 실데이터로 업데이트합니다.
- 고해상도 ERD(PDF/PNG) 또는 원본 파일(예: ERwin, eXERD, draw.io 등)
- DB DDL 스크립트(테이블/인덱스/제약 포함)
- 대표 조회/등록/통계 SQL 5~10건(실제 접근 패턴 파악용)

## 12) 즉시 적용 가능한 권장사항(가설 기반)
- 허브형 대형 테이블이라면:
  - 필수/선택 컬럼을 분리해 “기본 정보”와 “확장/옵션 정보” 테이블로 수직 분할 검토
  - 코드값은 전용 마스터 테이블로 통합, FK+인덱스 부여
  - 상태/이력은 별도 이력 테이블로 관리하고, 현재 상태는 허브에 캐시(이중화 규칙 명시)
- 빨간색 테이블(이상 징후/중요도 표시 추정):
  - 데이터 품질 규칙/제약 강화를 우선 검토
  - 삭제/승인/정산 등 민감 이벤트라면 아카이브/감사 로그 분리

---

작성자 메모:
- [ ] 고해상도 ERD/DDL 수신 대기
- [ ] 엔터티/컬럼/키 정확 명칭 채우기
- [ ] 핵심 쿼리 수집 후 인덱스 설계안 제시
- [ ] 품질 게이트(무결성/성능/보안) 점검표 완성
